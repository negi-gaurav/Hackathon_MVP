services:
  backend:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app/backend
      - ./models:/app/models
    environment:
      - DEBUG=true

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
